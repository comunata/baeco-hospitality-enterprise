/**
 * Plain constants shared between server-only admin code (lib/admin/
 * session.ts) and client components (app/admin/error.tsx) — kept in a
 * file with no "server-only" import so it can safely cross that boundary.
 */
export const DEMO_WRITE_BLOCKED_MESSAGE = "Acesta este un cont Demo. Modificările nu sunt salvate.";
